# Optional: GitHub personal access token to increase the API rate limit
# when fetching contributor data for the examples section.
# Vercel automatically provides VERCEL_URL at deploy time – you do not
# need to set it manually.
GH_AUTH_TOKEN=
